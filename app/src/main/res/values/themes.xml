<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Compose owns the real colors; this theme only sets the window shell
         (background, no action bar) for light mode. values-night overrides it. -->
    <style name="Theme.Lingua" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
    </style>
</resources>
